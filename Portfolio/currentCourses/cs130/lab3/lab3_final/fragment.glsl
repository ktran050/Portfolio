varrying vec3 lightDir,normal,pos;
void main() {
  vec l,n, eye;
  vec4 color;

  // compute ambient component
	// Color of object * Color of light
  // compute diffuse component
	// 
  // compute specular component

  gl_FragColor = color;
}
