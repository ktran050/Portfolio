#include <iostream>
using namespace std;

template < typename T >
struct Node{
    T val;
    Node < T >* next;
    // typedef string T;
    // typedef allows it so that we can then use T as a string
}

int main(){
    
    return 0;
}